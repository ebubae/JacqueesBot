class Sample:
	def __init__(self, name, instrument, length, is_pitched, pitch, embedding):
		self.name = name
		self.is_pitched = is_pitched
		self.pitch = pitch
		self.instrument = instrument
		self.length = length
		self.embedding = embedding
		

